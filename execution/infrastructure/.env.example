# ==========================================
# All In Academy Bot — Environment Variables
# ==========================================
# Скопировать в .env и заполнить реальными значениями
# НИКОГДА не коммитить .env в git!

# ------------------------------------------
# Telegram
# ------------------------------------------
TELEGRAM_BOT_TOKEN=               # Токен от @BotFather (формат: 123456:ABC...)
TELEGRAM_ADMIN_GROUP_ID=          # ID группы менеджеров (формат: -1001234567890)
TELEGRAM_MANAGER_USERNAME=        # Username менеджера для AI-редиректа (формат: @manager)

# ------------------------------------------
# OpenAI (GPT-4o mini + Whisper)
# ------------------------------------------
OPENAI_API_KEY=                   # sk-proj-...

# ------------------------------------------
# Zoom (Server-to-Server OAuth)
# ------------------------------------------
ZOOM_ACCOUNT_ID=                  # Из Zoom Marketplace App
ZOOM_CLIENT_ID=                   # Из Zoom Marketplace App
ZOOM_CLIENT_SECRET=               # Из Zoom Marketplace App

# ------------------------------------------
# Google (Sheets + Calendar)
# ------------------------------------------
# JSON ключ Service Account в одну строку (скопировать содержимое .json файла)
GOOGLE_SERVICE_ACCOUNT_JSON=      # {"type":"service_account","project_id":"..."}
GOOGLE_SHEETS_ID=                 # ID таблицы из URL (docs.google.com/spreadsheets/d/ID/...)
GOOGLE_CALENDAR_ID=               # ID календаря (обычно email@gmail.com или primary)

# ------------------------------------------
# Email (Resend)
# ------------------------------------------
RESEND_API_KEY=                   # re_...
RESEND_FROM_EMAIL=                # hello@allinacademy.ee
RESEND_FROM_NAME=                 # All In Academy

# ------------------------------------------
# Redis
# ------------------------------------------
REDIS_URL=                        # redis://redis:6379 (в Docker) или redis://host:6379

# ------------------------------------------
# Application
# ------------------------------------------
NODE_ENV=                         # development | production
APP_PORT=                         # 3000

# Домен для Telegram webhook (https://yourdomain.com)
WEBHOOK_HOST=                     # https://allinbot.example.com

# Секреты для валидации входящих запросов
TILDA_WEBHOOK_SECRET=             # Случайная строка (min 32 символа)
INTERNAL_SECRET=                  # Другая случайная строка (min 32 символа)

# ------------------------------------------
# Bot settings
# ------------------------------------------
BOT_LINK=                         # https://t.me/YourBotUsername
WELCOME_VIDEO_FILE_ID=            # file_id видео-кружка (получить после загрузки)

# ------------------------------------------
# Генерация секретов:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ------------------------------------------
